# train.py torch.amp.GradScaler(device, ...) / autocast(device, ...) cihaz-
# parametreli API'sini kullanıyor — bu, torch>=2.3.0 gerektirir. Alt sınır
# 2.1.0 iken bu API import hatası veriyordu (bu oturumda tespit edildi).
# facenet-pytorch 2.6.0 metadata'sı torch<2.3.0 ister ama torch 2.3+ ile
# FİİLEN çalışıyor (doğrulandı) — bu yüzden facenet-pytorch üst sınırı
# zorlanmıyor, sadece torch/torchvision alt sınırı yükseltildi.
torch>=2.3.0
torchvision>=0.18.0
facenet-pytorch>=2.5.3
insightface>=0.7.3
onnxruntime>=1.16.0
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0
tqdm>=4.66.0
tensorboard>=2.14.0
torchmetrics>=1.2.0
pytorch-msssim>=1.0.0
timm>=0.9.0
# Opsiyonel: config.loss.use_lpips=True iken gerekli (Kaggle'da:
# !pip install -q lpips)
lpips>=0.1.4
